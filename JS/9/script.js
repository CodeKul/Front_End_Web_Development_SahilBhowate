//object
//In Javascript programming language ; an object is a collection of name:value pairs.
//These "name:value pairs" are also known as "key:value pairs" OR "property:value pairs."
//"Pair is a set of two similar things."

// Example of key value pairs is
//Word : WordMeaning pairs in the english dictionary.

//Object is a non-primitive data type.

//Example of object in JS
let user = {
  userName: "Rick",
  age: 33,
};

//How to access value of a particular property in a JS object ?
console.log(user);

let userNameVal = user.userName;
console.log("The userNameVal is ", userNameVal);

let ageVal = user.age;
console.log("The ageVal is ", ageVal);
